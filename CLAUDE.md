# 项目协作约定

## 默认委派给 quant-expert

在本仓库中处理用户请求时，**默认通过 Agent 工具调用 `quant-expert` 子代理**来完成实际工作（代码阅读、修改、调试、回答领域问题等），除非满足以下任一例外：

- 用户明确指示不要使用子代理，或指定了其他 agent。
- 任务与本项目领域（期权定价、对冲回测、Monte Carlo、Wind 数据、gui_app）完全无关，例如纯粹的 git 操作、修改 `.claude/` 配置、环境排查等。
- 任务只是一次性、极小的查看（如读取单个文件的几行）——此时直接用主代理的工具更高效。

调用时在 prompt 中给出足够自包含的上下文（目标、相关文件路径、已知约束），不要让子代理从零猜测意图。子代理返回后，主代理负责向用户汇总结论与改动。
