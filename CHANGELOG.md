# 更新日志

本文件记录 DeltaLab 每个发布版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

预编译包在 [Releases](https://github.com/Grefer/DeltaLab/releases) 页下载。

---

## [未发布]

### 性能

- **策略优选提速约 6.6 倍，定价结果逐位不变。** 同一段行情上，各候选策略的 bar 级定价本来就完全相同（定价是 `(期权, 价格前缀, 每日 bar 数)` 的纯函数，策略只决定哪些 bar 额外要 Greeks），此前 8 个候选各算了 8 遍。现在在段作用域内共享。实测气囊 nPath=100000、GUI 默认配置（含收盘兜底对冲）下两轮分别为 6.88x 与 6.57x，定价次数降到 15.2%，整张排名表逐位一致。
- 顺带消掉两处旧开销：Greeks bump 的对象复制从 `deepcopy` 改为浅拷贝 + 逐个复制可变容器（从 O(已实现天数) 变成 O(1)）；策略上下文里的对数收益历史由每根 bar 重新拼接改为预拼一次取切片视图（去掉 O(n²) 拷贝）。两者均不改变任何输出。
- **分段并行，短期限结构再快约 1.4 倍。** 各分段彼此独立（自己的行情切片、自己的 Day 0 锚点、自己的定价缓存），现在并行跑；归并仍按分段顺序串行做，因此排名逐位不变。线程数上限 4：实测标度曲线在 4 之后就平了（1 线程 1.00x、2 线程 1.27x、4 线程 1.38x、6 线程 1.41x、8 线程 1.37x），这活受限于内存带宽而非算力，开满反而更慢；单次定价峰值内存过大的配置会进一步自动退让（可用 `DELTALAB_SELECTION_MEM_BUDGET_MB` 调整，默认 2048 MB）。
  - **只对期限较短的结构有效。** 分段数等于「证据天数 ÷ 合约期限」，所以 T=20 时最长的一档有 13 段、并行跑得满，而 T=243 时每档都只有 1 段，并行度为 1、没有任何收益。雪球这类长期限结构的提速全部来自上面的定价共享。要让它也并行，得把不同周期档的分段摊平到同一个池子里跑，那是另一次改动。
- **MC 内核与累计期权 payoff 大幅省内存（不改数值）。** `McGbmQ` 改为全程在一块缓冲上原地推进，峰值从结果矩阵的 4.5 倍降到 1 倍（nPath=100000、243 步时 834 MB → 185 MB）；累计期权的贴现因子此前被 `np.tile` 物化成 nPath × 观察日的矩阵，而它每一行都一模一样，改成广播后 `Opt_Decumulator` 的单次定价峰值从路径矩阵的 6.25 倍降到 5.25 倍（累计族里最高的是 `Opt_EnDecumulator` 的 7.00，各子类型不同）。
  - **这两项是省内存，不是提速。** 完整优选链路上背靠背 A/B 七轮，中位数之比 1.01x，落在噪声里——原地写法仍要对同一块数组做五趟读写，带宽流量几乎没变，省掉的是分配。收益在于分段并行的内存预算宽裕了，以及削掉了每次定价里那块沿路径维恒定的贴现矩阵。
- **Wind 日线加了本地缓存，重复取数不再走网络。** 日内分钟数据本来就有缓存，日频没有——而策略优选的品种池模式恰恰要逐个具体合约拉日线，换一个参数重跑就把整串请求原样重来一遍。同一段已收盘的历史日线不会再变，没有理由每次都问 Wind。现在按 `代码 + 起止日 + 复权口径` 落 parquet（与日内同一个缓存目录），命中耗时约 **1 ms**（一年 260 根、文件 5.6 KB，预热后 9 次的中位数），读回的序列与首次逐位一致。
  - 复权口径进了 key：`F` / `B` / 不复权三种各存各的，不会混用——否则读回来的序列复权方式与调用方预期不符。区间不同也不共用。
  - 缓存目录不可写时只是退化成每次都重新取，不影响本次结果，但会记一条日志——否则「每次都重拉」会静默发生，只表现为「怎么又这么慢」。

### 新增

- **有运行日志了。** 免安装版是 `console=False` 无终端运行的，而此前运行期诊断全靠 `print(..., file=sys.stderr)`——冻结进程里 `sys.stderr` 是 `None`，那句话不报错也什么都不写，用户报「我这儿特别慢」时现场零信息。现在落到 `~/.deltalab/logs/deltalab.log`（开发态 `data/logs/`），2 MB 滚动、留 3 份、封顶 8 MB。记的内容：启动与退出、每一轮策略优选的全部输入（来源 / 标的 / 区间 / 粒度 / 结构 / 周期 / 头寸规模与成本）、每档周期的领先候选与累计耗时、中止与完成的总耗时，以及所有异常的完整 traceback。
  - 日志目录不可写（权限、磁盘满）时只是退化成不记，绝不打断主流程。
  - 加了一条 AST 守卫测试，防止诊断再退回 `print(file=sys.stderr)`。
  - **`pricing/` 里的三处诊断也接了进来**，此前它们各自漏在外面：`wind_data.py` 缓存目录不可写是一句裸 `print`（冻结包里同样静默丢失）；`trade_calendar.py` 交易日历退化成自然日、`hedge_backtest.py` 结算日历缺失，走的是 `warnings.warn`——只进 Python 的告警栈，不落盘，而且默认每个位置只响一次。三处现在都同时保留原有行为并补一条日志（`warn` 没删：调用方可能拿它做断言）。
  - `pricing/` 用 `logging.getLogger("deltalab.xxx")` 直接取子 logger，**不 import 根目录的 `deltalab_log`**——定价层不该反向依赖 GUI 层。子 logger 会在 `deltalab_log.setup()` 配好 `"deltalab"` 之后自动继承处理器，没 setup 时（比如 pytest 只跑纯逻辑）什么都不写，不会凭空在仓库里造出日志文件。

- **策略优选有了真实进度**：进度条与剩余时间按**成本份额**推进，分母在开跑前精确算出。此前是一条来回滚动的假进度条，而任务要跑十几分钟。
  - 份额不是按「跑完几个分段」算的，那会严重失真：分段数与工作量不成比例（`L ≤ T` 时一档恒为一段，无论这档是 5 天还是 243 天）。实测气囊 T=243、五档全选时，旧口径在只跑完 1.8% 墙钟时就把进度条推到 20%。
  - 也不能简单按段长加权。单次 MC 定价的成本 ∝ 剩余期限，而期权每过一天剩余期限就少一天，所以一段的成本是 `L·(T − (L−1)/2)` 而非 `L`。实测（雪球 T=243 逐段计时，相对最短段）段长 5/20/61/122/243 的真实耗时比是 1.00/3.40/9.24/16.06/21.13，按段长加权在最长档高估 **130%**，本式误差恒定在 +15% 左右——形状对了、只差一个常数比例，而进度条只看相对份额。
  - 效果：进度条份额与真实墙钟之比的中位数从 **2.64 降到 1.13**。
- **策略优选边跑边出结论**：每跑完一档周期，就把它的领先候选报到状态栏（「已出结果 —— 近一周：1.5σ；近一月：C2C」）。这一档的名次此后不会再变——名次按周期分组算、周期之间零耦合，而五档是从便宜到贵跑的：跑完 **6%~8%** 就有两档最终结论，**47%~58%** 有四档（具体比例随合约期限变，短期限偏前者、长期限偏后者）。配合上面的中止，看到结论已经明朗就可以直接停，不必等满十几分钟。
- **策略优选可以中止**：跑起来之后「▶ 开始优选」变成「■ 停止优选」。这是**完全停止**而不是暂停——不保留部分结果、也不能续跑，但页面上已有的上一轮结果不受影响。停止发生在当前**候选**跑完之后（不是分段），因此延迟约等于一个候选的定价耗时：短期限结构几秒（实测气囊 T=20、4 线程下最坏 6.2 秒），长期限结构可能要几分钟（累计 T=243、nPath=100000 时单个候选就要 6 分半）。在回测内部插更细的检查点会留下半截状态，没有那样做。
  - **取数阶段同样可以中止。** 品种池模式在正式开跑前要逐个具体合约拉 Wind 日线，这段循环此前没有检查点——按钮已经显示「■ 停止优选」，点下去却毫无反应，而合约多时这一段本身就要等上好一会儿。现在每个合约之间检查一次：实测 5 个合约的池子，在第 3 个之后请求中止，拉满 3 个即停；开跑前就置位的话一次 Wind 请求都不会发出。
  - 取数阶段也顺带有了进度：状态栏按「第 i / n 个合约 + 合约代码」刷新（200 ms 节流）。这一段不并入主进度条的分母——它的耗时取决于 Wind 的响应而非计算量，混进成本份额只会把后面那条本来准了的进度条重新搅浑。

### 变更

- **左侧表单的「大类」「子类型」与下方各行不等宽。** 输入列此前 168px，装不下最长的结构名「敲出计零·区间固赔·到期杠杆累计」（实测 189px 文本 + 26px 下拉箭头内边距 = 215px），于是那两行被单独放宽成跨两列——同一张表里出现两种宽度。现在把输入列统一加宽到 216px，两行回到普通单列，左面板初始宽度同步加宽以免挤掉右侧说明列。

- **⚠️ 熔断累计期权的定价口径改了，价格会变。** 熔断之后的结算此前有两套互相矛盾的算法：熔断日已经落在已实现历史里时，走「冻结在熔断日价格 × 剩余天数」；熔断日落在模拟段时，`ASGQ_EP`/`ASGQ_DP` 按**当天**价格逐日结算、`ASGQ_call_put` 按**到期**价格结算。同一条路径只把熔断日从模拟段挪进已实现段，价格能差十倍——而对冲回测每天都在往已实现段追加收盘价，标的一穿越障碍，当天估值就从一条路径跳到另一条。
  - 现按实务口径统一：**熔断当天直接结算**，熔断日及之后每天的收益都等于「熔断日价格 − 保障价格」这个常数，期权随即终止。也就是把 MC 分支对齐到本来就正确的早退分支。
  - 贴现同步对齐：熔断腿是一整笔在熔断当日付清，因此整笔按**熔断当日**的因子折回 0 时刻，而不是各天用各天的因子（每日结算的累计腿仍逐日贴现）。**模拟路径的熔断腿仍然要折现**——「熔断当天不贴现」只适用于已实现行情序列里的熔断，那种情形下钱当天就到手了。整段保持向量化，用 `np.argmax` 一次取出所有路径的首个触发列。
  - **`ASGQ_call_put` 的未熔断腿改为到期一次折现**。该结构按保证金逐日盯市估值，但现金流当作到期一次结清处理。此前它的三条腿并存三套折现口径（基腿逐日、杠杆腿到期、熔断腿熔断日），导致标的擦过障碍时凭空跳 1.49%——熔断的只有一天，换掉折现口径的却是全部 243 天，多出 `(S_T−K)·(Σdf_j − le·df_T)`。统一后障碍处只剩真实经济差。该项影响未熔断路径：20~61 日约 −0.1%，243 日 +2.6% ~ +3.3%（r=3%~5%）。
  - **影响面：七个熔断结构全部。** `ASGQ_EP` / `DP` / `call_put` 的熔断后金额依赖价格，改动最大；`ASGQ_EF` / `DF` / `EFF` / `DFF` 付的是常数「熔断赔付」，金额不变，但**结算日**同样要对齐——它们此前把这笔钱逐日铺开贴现，与早退分支矛盾，现在也整笔在熔断当日折回。
  - 缓存加了 `PRICER_VERSION`：此前 key 的七项刻画的全是**输入**，没有一项能察觉「定价代码换了口径」，旧口径的逐 bar 缓存会被当成有效命中原样读回。改数值时这个版本号必须 +1。本机 145 条旧缓存已清空。
  - **价格变化幅度**：`EP`/`DP`/`call_put` 在短期限低波动下几乎不变（20 日 × σ=0.15 时 < 0.1%），期限和波动率上去之后变化明显（122 日 × σ=0.35 时 −27.5% ~ +31.6%）。`EF`/`DF`/`EFF`/`DFF` 只有贴现日变化，幅度小得多（61 日 0.02%、243 日 × r=0.05 约 0.30%），但形态上是**熔断当日的一次跳变**，会在对冲回测里制造伪 P&L。
  - **存量记录会对不上**：这三个子类型的历史回测结果、优选记录与逐 bar 缓存都是旧口径算的，需要重跑才能与新价格比较。

- **累计期权 13 个结构的中文名整套重排，并修掉其中一处指错对象的说法。** 旧名是历史叫法的堆叠：修饰语想放哪放哪（「固定赔付回归累计」前置、「固赔到期结算累计」中置、「熔断每日保障累计」夹在中间），排序后同族排不到一起；「固赔」二字在同一个下拉里指三样东西（`Decumulator_Fix` 的是 `fix`、`ASGQ_EF` 的是 `amount`、`*FF` 的「双固赔」是两个都有）。现在按三个正交维度拼，顺序固定，取默认值的那一段省略不写：

  | 维度 | 取值 |
  |---|---|
  | ① 触碰障碍 H 之后（必写） | `敲出终止` / `敲出计零` / `敲出增强` / `熔断保障` / `熔断赔付` |
  | ② 区间 `(K, H)` | 线性省略；按 `fix` 结算写 `区间固赔` |
  | ③ 杠杆腿 `S ≤ K` | `每日杠杆` / `到期杠杆`（`到期结算` 仅 `ASGQ_call_put`，它连主项也只用到期收盘价） |

  对照表见 [`docs/GUI_USAGE.md`](docs/GUI_USAGE.md) 4.1 节。举例：`Opt_Decumulator` 从「普通累计」变成「敲出终止累计」，`Opt_ASGQ_DFF` 从「每日熔断双固赔累计」变成「熔断赔付·区间固赔·每日杠杆累计」。子类型下拉的顺序也跟着重排，让 `ASGQ_D*` 与 `ASGQ_E*` 两两相邻。

  - **③ 这个维度此前是错的。** `ASGQ_E*` 与 `ASGQ_D*` 的注释写作「到期观察熔断」与「每日观察熔断」，但七个 ASGQ 的熔断判定都是同一行 `np.cumsum(ss >= H, axis=1) > 0`，逐日路径依赖、完全一致——真实差别只在杠杆腿：E 只在到期日看 `S_T ≤ K` 并按累计天数一次性补 `(N − 1)` 倍，D 逐日看 `S ≤ K`、当日乘 `N`。于是「熔断每日保障累计」(EP) 与「每日熔断保障累计」(DP) 不但只差两字语序、在下拉里认不出，两个还都修饰错了对象。**这只改注释与显示名，不动任何 payoff，定价逐位不变。**
  - **不需要迁移任何已有数据。** 已保存的优选结果本来就是读盘时按内部键现算显示名；结果池的 `option_label` 确实落了盘，但同一个包里的重放配方存着内部键，改为载入时重算即可，老包会跟着改名——不会出现同一张表里新旧两套名字并存。只有重放配方为空的包（`prices.size < 2`）退回落盘的旧名。
  - **说明卡的标题行改为由 `SUBTYPE_DISPLAY` 生成。** 此前它是手抄的第二份副本，13 条累计结构已经漂开 5 条（`ASGQ_call_put` 显示名是「到期熔断保障累计」而说明卡写「到期观察熔断保障累计」）。现在有测试锁死两者逐字相同。
  - **旧名仍能反查回内部键**（`SUBTYPE_FROM_DISPLAY` 并入了一张别名表），手输或从旧文档粘贴不会失效；但界面各处一律显示新名。
  - 最长的名字从 9 个汉字变成 14 个汉字加 2 个间隔点，因此两处版面跟着调：左侧表单的「大类 / 子类型」两个下拉溢出到本来就空着的说明列（输入列 168px 装不下，而改输入列宽会连带压窄说明列，其余各行的等宽对齐不受影响）；「已保存的优选结果」窗口的子类型列 150 → 210px（该表用 TkDefaultFont，实测 189px）。结果池那张表不用动——它的单元格是 Menlo 9，同一串只要 147px，原本的 150px 上限正好装得下。

- **累计期权的两个赔付类参数改名，并给保障价格一个可用的默认值。** `fix` 与 `amount` 此前叫「固定赔付」和「固定金额」——两笔完全不同的钱，名字却看不出差别，代码注释里更是两个都写成「固定赔付」。现在按各自的触发条件命名：
  - `fix` → **区间赔付**：标的落在 K~H 区间时每日结算的金额
  - `amount` → **熔断赔付**：熔断日起每日结算的金额
  - `P`（保障价格）默认值 `0.0` → **`100.0`**（与默认入场价一致）。此前默认 0 会被当成「未填写」，于是「熔断保障·到期结算累计」等三个结构开箱即报错；现在它们可以直接定价。后来查明 `fix` / `amount` 填 0 本就合法（见「修复」一节），三项一并不再做 falsy 转换，**默认参数下 18 个子类型全部可定价**。
  - 结构显示名本条未改，但下一条把它整套重排了——当时判断的「改名会让新旧记录对不上」并不成立，见下。

- **⚠️ 跨合约归一化的分母换了口径，schema 升到 `s0_x_abs_quantity_v2`。** 旧分母是 `S0 × multiplier × |quantity|`，而 `multiplier` 的实际作用只是把目标持仓向零取整到整手——持仓是 `round(方向 × Δ × 数量 / 乘数) × 乘数`，乘数在里面约掉了，量级完全由数量决定。于是 PnL 只随数量放大、分母却随乘数放大，两者不同步。
  - 实测同一份持仓（`quantity=10000`、Day 0 持仓 5088.5、PnL≈4340），乘数取 0.01 / 0.02 / 0.05 时成交与盈亏几乎逐位相同，旧口径的归一化值却是 4.34e-1 / 2.17e-1 / 8.68e-2，差 5 倍。新口径三者同为 4.340e-3。
  - **按默认乘数 5，新口径的归一化数值是旧口径的 5 倍。** 金额列、持仓、成交、排名全部不受影响——只有带 `normalized_` 前缀的曲线与分母元数据会变。快照各自记着 schema，跨版本比归一化列前要先对 schema。
  - 顺带修好一个副作用：`合约乘数 = 0`（连续对冲）此前会把分母算成 0、让整个归一化 fail closed，而连续对冲恰恰是最干净的一组基准。新分母与取整粒度无关，0 是完全合法的输入。

### 修复

- **交易日历不够用时会返回一段被悄悄截断的区间。** 仓库预置的 `data/tradingday.csv` 覆盖到 2026-12-31，超出范围又联网刷新失败时，旧逻辑是 `warnings.warn` 后沿用旧文件——实测查 2026-12-28~2027-01-08 只返回 4 天。而那句告警在免安装版里根本不存在：`console=False` 的冻结进程 `sys.stderr is None`，`warnings.warn` 是个静默 no-op，"已经告过警"只是开发机上的错觉。现在覆盖不足直接报错，并写清可用区间到哪天、该替换哪个文件。
  - **抓取成功但写盘失败不再作废整次刷新。** `_write_local` 的异常此前一路冒到 `load_calendar`，把"已经拿到正确日历"变成一次彻底失败。现在降级为告警，本次照常使用刷新结果。
  - **冻结态的回写目标改到 `~/.deltalab/data/`。** `.app` 包与安装目录可能只读，而结果池、逐 bar 缓存、运行日志三处早就是这个约定，交易日历是仓库里唯一还写在模块旁边的例外。读取时可写副本优先于随包那份。

- **单次回测的「剩余期限」在模拟 / CSV 来源下完全没有校验，`T=0` 会跑出一份假报告。** 校验只写在 Wind 分支（`resolve_single_wind_state` → `maturity_days_from_params`），另外两条来源直接绕过去；而 `_build_backtest` 里 `params.get("T_days") or params.get("T") or 20` 把 falsy 的 0 悄悄兜成了 20。实测填 `T_days=0` 会得到：`n_trade_days=20`、期权价值逐日随价格跳动（那是已到期期权的内在价值）、Delta 恒 0、PnL 恒 0——一份看上去完全正常、实际没有任何意义的报告。`T_days=-5` 则抛 numpy 的 `negative dimensions are not allowed`。现在校验前移到数据来源分流之前，三条来源共用同一把尺子。

- **`multiplier` 与 `contract_multiplier` 缺少有限性校验，非法值不报错而是静默改变行为。** 同一个构造函数里 `tc_rate` / `slippage_bps` / `quantity` / `position` 都有 fail-fast 校验，只有这两个漏了，而 GUI 的「合约乘数」是自由文本输入框。
  - `multiplier=-5` 走进 `mult <= 0` 分支，与 `0` 给出**逐位相同**的持仓和 PnL——用户以为设了 5 手一档，实际在做连续对冲，界面上没有任何区别可看。
  - `multiplier=inf` 让 `int(target/inf)*inf` 变成 `0*inf = NaN`，整条持仓与 PnL 全 NaN 却照样返回结果；`nan` 则一路漏到 `int()` 才炸，报的是 `cannot convert float NaN to integer`，与输入项对不上。
  - `contract_multiplier` 取 `0` / 负数 / `nan` 时 `if is_fut and cmult > 0` 判否，**静默退回非期货取整分支**——一次期货回测悄悄地不是期货回测，而结果里 `is_future` 仍写着 `True`。
  - 现在两者都在构造期点名报错（`multiplier` 允许 `0` 或有限正数，`contract_multiplier` 要求有限正数），`_compute_target` 里那条已不可达的隐含兜底一并去掉。

- **结果池的删除 / 重命名失败后界面仍报成功。** 删除是先把快照从内存池 `pop` 掉、再忽略文件删除的返回值，于是只读目录或文件被占用时，这条结果从界面上消失、状态栏写着「已删除」，文件却还在盘上，下次启动它自己又回来了。现在改成**先删文件、再动内存**：删不掉就原样留在池里，并弹框点名是哪几条（单条删除、多条删除、全部清空三条路径都覆盖）。
  - 重命名此前 `except Exception: pass` 全吞。名字在本次会话里确实改好了，但重启会变回去，而中间没有任何一处提示过。现在把失败抛给调用方，界面提示「重命名未写入结果文件……重启后会恢复为旧名称」，且不会因此重新弹出改名对话框。
  - 「历史结果」页的删除同样忽略了返回值。虽然紧跟的 `refresh()` 会把删不掉的那条重新列出来，但列表里凭空多回一行不会被读成"删除失败"，现在明说。
  - 两处都把「文件本来就不存在」与「删不掉」区分开——`delete_snapshot` / `delete_result` 对两者都返回 `False`，但前者的目标状态已经达成，该照常出池。

- **滚动回测记录的建仓日晚了整整一个交易日。** `real_s0` 取自 `prices_arr[t0]`，`start_date` 却写 `log_ret.index[t0]`，而对齐关系是 `log_ret.index[k] ↔ prices_arr[k+1]`。实测建仓价 104.0（对应 2026-01-07）被记成 2026-01-08。现在按与价格同一格的日期轴取值。（`run_rolling_backtest` 目前没有 GUI 调用点，只有测试在用，因此不影响任何界面结果。）

- **一日回测的滚动已实现波动率是 `[0, NaN]`，外带两条 RuntimeWarning。** 窗口只有一个收益时 `np.std(ddof=1)` 返回 NaN。这不是构造出来的边界：严格历史拆分本来就会留下一日残段（`_strict_lookback_segment_lengths` 的 `L=243, T=22 → (1, 22×11)`）。现在与同一段里 `realized_vol` 的 `n > 1` 判据取齐，样本不足时统一给 `0.0`。全量非 GUI 测试的警告数由此从 62 条降到 6 条——那 56 条噪声正是会掩盖真实边界问题的东西。

- **theta 的 CRN 是断的，显示出来的值基本是噪声。** 有限差分求 theta 时把剩余期限减一天，`nStep` 随之从 T 变成 T−1，`standard_normal` 换了形状、抽出**完全不同**的一批随机数——theta 于是成了两次相互独立的 MC 估计之差。`McGbmQ` 新增 `draw_steps` 参数：bump 也按 T 抽、只用前 T−1 列，两边共享同一批随机数。
  - 实测跨 16 个 seed 的 theta 标准差：气囊 5.86 → 3.29、雪球 38.33 → 7.61、累计 134.55 → 12.24、亚式 4.94 → 0.42（降 1.8~11.6 倍）。
  - **气囊与雪球此前连正负号都是错的**（气囊均值 −0.374 → +1.577，雪球 −8.523 → +8.081）。
  - delta / gamma / vega / rho **逐位不变**——它们的 bump 不改 `nStep`，本来就共享随机数。theta 也从不进入 PnL 或排名，所以存量结果的排名一个数都不会变。
- **策略优选渲染失败会丢掉整轮计算。** 结果此前只活在回投主线程那个闭包里，展示层一抛异常，十几分钟的计算连同 gzip 后仅一百多 KB 的结果包一起没了，只能重跑。现在 worker 在投递前先把结果扣在手里，渲染失败时询问是否存成结果包（可在「载入」里打开），拒绝或成功后都会清掉暂存。

- **`mc_seed=` 写在构造参数里会被静默吞掉。** 它是 `OptionBase` 的类属性而非各子类 `__init__` 的形参，于是 `Option_DE(..., mc_seed=7)` 掉进 `**kwargs` 里——不报错、也不生效，所有实例继续共用同一批随机数。写「不同 seed 跑多次取标准差」的脚本最容易在这里翻车：标准差恒为 0，看着像是定价稳得离谱。现在 `mc_seed` 与 `greeks_nPath` 都能在构造期设置，**其余未知参数一律报 `TypeError` 并点名**（此前五个类的 `**kwargs` 从未被读过，纯粹是个静默丢弃口，连参数名拼错都没有任何提示）。

- **累计期权的「已过天数」补上了配对的「已实现序列」输入，不再是死路。** 该字段一直可编辑，但建构闭包把已实现价格序列硬编码成空，于是一填非 0 就在定价深处炸 `IndexError`（13 个子类型无一幸免），界面上又无处补那几天的收盘价。现在两者成对出现：填了几天已过，就填几天收盘价（逗号 / 空格 / 中文逗号都能分隔）。默认仍是 `0` 与空，行为与从前完全一致。
  - 长度对不上会点名报错：「已实现价格序列长度（2）必须等于已过天数（3）」。
  - 参数详情窗对**合约参数组**豁免了「空值整条略过」的规则——详情是照着它重建合约的依据，「已实现序列」留空是合法取值而非缺失，会显示成 `—` 留在表里。

- **切换看涨/看跌后障碍档位仍留在原侧，算出来的全是「首日敲掉」的退化值。** 累计 / 气囊 / 雪球的触发条件都会随方向翻转（累计看涨 `S ≥ H` 熔断、看跌 `S ≤ H`），但默认档位只朝一个方向摆。实测切到反方向后首日触发概率 **1.0000** —— 界面照常给出数字，看不出异常。现在两条一起补：切换方向时，**仍是默认值**的价格档位自动绕初始价镜像到另一侧（用户改过的一律不碰）；提交时校验障碍是否已在标的价的错误一侧，是就点名报错。
- **累计期权填了「已过天数」就必崩（13 个子类型全部）。** GUI 的建构闭包把已实现价格序列硬编码成空，观察日却按「剩余期限 + 已过天数」生成，于是掩码矩阵比价格矩阵宽，在定价深处炸出只有两个数字的 `IndexError`。现在改为在构造期校验「已实现天数 + 剩余期限 = 观察日数」并点名报错。**注意这只是把崩溃变成看得懂的提示**——GUI 目前没有填写已实现价格的地方，所以「已过天数」仍然只能填 0。
- **1 天期的累计期权在看跌方向会静默算错 `nPath` 倍。** 观察日只有 1 天时，形状 `(nPath,)` 与 `(nPath, 1)` 会广播成 `(nPath, nPath)` 而不报错，价格随路径数线性放大——GUI 默认 `nPath=100000`，即错 10 万倍，同时试图申请一块 80 GB 的数组。与下面那条同一个根因（看跌分支漏 `.reshape`），一并修掉。
- **累计期权的「熔断保障·到期杠杆累计」与「熔断赔付·到期杠杆累计」（旧名「熔断每日保障累计」/「熔断每日固赔累计」）选看跌方向直接崩溃。** `Opt_ASGQ_EP` / `Opt_ASGQ_EF` 的看跌分支漏了一处 `.reshape(nPath, 1)`，抛 numpy 广播错误；看涨分支一直是对的，修法就是照抄它。这两个结构此前在看跌方向完全不可用，所以修复不会改动任何既有结果——但**产出的看跌价格建议自行核对一遍**，它们从未真正跑出来过。
- **累计期权 10 个子类型在默认参数下无法定价。** 建构闭包写的是 `fix=p["fix"] if p["fix"] else None`，而 `0.0` 是 falsy——**填 0 被悄悄当成了「未填写」**，于是 `Opt_Decumulator_Fix`、`Opt_ASGQ_*` 等结构在定价深处抛 `TypeError`。**0 是合法取值**：区间赔付 0 = 标的落在 K~H 区间那几天不结算，熔断赔付 0 = 熔断之后不再有现金流，都是真实条款。现在三项原样传入，默认参数下 13 个子类型全部可定价。
  - 表单里这三项恒有值，不存在「未填」这个状态；只有直接调用 `Option_DE` 却漏传参数才会拿到构造期的点名报错（判据是 `is None`，不是 falsy）。
  - 字段标签同时从「(可选)」改为「(部分结构必填)」——它们对部分结构是必需的，但**必需的是「有值」，不是「非 0」**。

### 构建与发布

- **发布流水线不跑测试就能发正式包。** `release.yml` 的 build 从 checkout 直接进 PyInstaller、release 只 `needs: build`，于是一条测试全红的 tag 照样能发出去。`tests.yml` 虽然存在，但它只在 push / PR 上触发，与 tag 发布毫无关系。现在 `tests.yml` 加 `workflow_call`，release 以 `test → build → release` 串起来复用同一套测试（而不是抄一份 pytest 步骤——抄一份意味着两条流水线的测试范围会慢慢分叉，而发布这条恰恰最不该跑得比主干松）。
- **macOS `.app` 的版本号写死在 `1.0.0`。** 仓库当时已经打到 v2.0.0，用户在 Finder 简介或系统报告里看到的版本与下载的包对不上，报 bug 时给出的版本号也就没有意义；`CFBundleVersion` 更是整个键都缺。现在由 `deltalab.spec` 的 `_resolve_version()` 解析：`DELTALAB_VERSION` 环境变量（release.yml 从 tag 注入）→ 本地 `git describe` 的最近 tag → `0.0.0`，并削掉 `v` 前缀与 `-rc1` 之类后缀（`CFBundleShortVersionString` 只接受 1~3 段数字，带后缀会让 Finder 与 codesign 拒绝整个 plist）。

### 测试

新增 `tests/test_trade_calendar.py`（4 条）守住日历降级：覆盖不足必须报错而非返回短区间、报错要说清可用到哪天与该换哪个文件、抓取成功而回写失败时继续使用抓取结果、冻结态写用户目录且可写副本读取优先。另加 15 条回归：取整粒度与合约乘数的非法输入（负数 / inf / nan / 0）逐个点名报错、`0` 与有限正数照常可用、归一化分母不随取整粒度变化、一日残段的滚动波动率是 `0` 而不是 `NaN`（用 `np.errstate(all="raise")` 兜住，不靠人眼看警告）、三条数据来源都拒绝非正剩余期限、滚动回测的 `start_date` 必须与 `real_s0` 同格、快照删不掉时留在池里而文件本就不存在时照常出池、改名写盘失败会抛出且内存改名仍生效。

新增 `tests/test_pricing_memo.py`（160 条），全量 969 → 1129 条。另加 20 条命名不变量：结构名必须解析成 ①[·②][·③]累计 且分词出自受控词表、D/E 成对的三组除杠杆腿外逐字相同、任何显示名不得是另一个的前缀、说明卡标题与显示名逐字相同，以及结果池老包按内部键重算显示名。核心断言是逐位相等而非近似：定价共享若改动了哪怕 1 ulp 都必须失败。含一条穷举守卫——改动期权的任何一个公开属性，缓存身份都必须随之改变。

---

## [2.0.0] - 2026-08-16

把 DeltaLab 从「跑一次回测看结果」变成「把多次回测横向比出结论」：新增 **🆚 结果对比** 与 **🎯 策略优选** 两个页面，并把 `gui_app.py` 拆成分层的 `deltalab_ui/` 包。相对 v1.1.0 共 42 个提交、约 +39,500 行。

### 新增

- **🆚 结果对比**：回测后按需保留快照，勾选任意组合即时对照曲线与绝对指标（期末净损益、总成本、最大回撤、触发 / 成交次数、换手额）。切换勾选只读缓存，不重跑回测。快照自动存本机、重开仍在，最多 20 条。页面不替你下判断，只负责说清这次的变量差在哪一项。
- **🎯 策略优选**：用 CSV / Wind 真实历史严格连续回放近周 / 近月 / 近季 / 近半年 / 近年五档，固定以每日收盘 C2C 为基准，按增量收益 / 增量信噪比排名，可把胜出参数一键写回左侧。支持品种入口（`P.DCE` 按 Wind 逐日主力映射）。
- **智能行情粒度**：每日 bar 数由时间索引与交易 session 自动推导，Wind 请求粒度按策略自动选择。
- 分段 bar 级明细的内容寻址缓存；`.env.example` 本地凭据模板。

### 变更

- **`gui_app.py` 拆成 `deltalab_ui/` 包**（15 个模块）。`gui_app.py` 收缩到 1,691 行、保留为兼容入口，外部与测试继续写 `gui_app.XXX` 即可。
- **策略优选周期口径重写**：从「挑若干终点、各自向前回看」改为「从同一截止日向前严格连续回放最近 `L` 日」。
- **移除行情粒度的手工调粗** —— 调粗只会静默漏掉 bar 内触发，让结论偏乐观。
- 指标 `增量性价比` 改名 `增量信噪比`；Wind 回测改为「分析截至日」驱动；历史候选上限 5 → 8；候选带宽的 σ 统一取左侧输入值。
- 表单改用统一布局系统；主题 / 字体 / 布局初始化抽到 `deltalab_ui/theme.py`；按钮与标签视觉层次调整。

### 修复

- **免安装版在装有 Wind 终端的机器上仍报 `No module named 'WindPy'`**：CI 构建机没有 Wind，包里不含 WindPy。现在会扫描本机 Wind 安装并接进进程重试，扫不到时报错列出扫过的位置；也可用 `DELTALAB_WIND_DIR` 手工指定 Wind 目录（见[使用文档 §1.3](docs/GUI_USAGE.md#13-可选依赖)）。
- **内容寻址缓存的键计算缺陷**：不同输入可能命中同一份缓存，会串用别的段的 bar 明细。
- **品种池模式未显式降级排名口径**：跨合约金额被直接相加。
- 策略优选校验的 `UnboundLocalError`，以及排序键混用不同量纲导致的排名错误。
- 回放策略未按分段成功过滤、预热参数未持久化；状态栏打包顺序；历史页分割线定位；headless import 失败。

### 测试与 CI

测试从 23 条（2 个文件）扩到 969 条（13 个文件：440 纯逻辑 + 529 GUI）。CI 在 ubuntu × Python 3.10 / 3.13 上跑全量，GUI 批用 `xvfb-run`。

### 升级说明

- **无需迁移**：结果池与策略优选结果包都是本版新增，v1.x 不存在这两类文件。
- 两类结果包各带 `schema_version`，**版本不符时拒绝载入而不是硬渲染** —— 字段口径改过不止一次，用新代码渲染旧包会静默给出错误结论。
- 三个运行时目录（`data/cache/`、`data/backtest_pool/`、`data/history_results/`）已写进 `.gitignore`：结果池包内含 `csv_path` 全路径与 `wind_code`。
- CI 产出的发布包不内置 WindPy（GitHub runners 上没有 Wind 终端），改为运行时扫描本机 Wind 安装：装了 Wind 终端并设置过 Python 接口的机器可直连，装在非常规位置时用 `DELTALAB_WIND_DIR` 指定。见[使用文档 §1.3](docs/GUI_USAGE.md#13-可选依赖)。

各页面的完整口径、参数含义与常见报错见 [使用文档](docs/GUI_USAGE.md)。

---

## [1.1.0] - 2026-05-22

### 新增

- **雪球期权**（`Option_SNB`）与 `trade_calendar` 交易日模块。
- 累计期权新增 `Opt_Decumulator`（普通累计）与 `Opt_Decumulator_Fix_E`（固赔到期结算累计）两种定价方法。
- PyInstaller 打包支持 WindPy：新增 runtime hook 确保冻结包里 DLL 能正确加载，并给出详细的错误提示。

### 修复

- Windows 打包版因 `charmap` 编码崩溃：spec 中强制 stdout 使用 UTF-8。
- 强制 JavaScript action 使用 Node 24，消除 Node 20 弃用警告。

---

## [1.0.0] - 2026-04-21

首个公开发布。

- 基于 Python / tkinter 的期权 Delta 动态对冲回测 GUI。
- 5 大类期权（香草 / 累计 / 亚式 / 气囊 / 雪球）、3 种数据源（蒙特卡洛 / CSV / Wind API）。
- 6 宫格对冲图表、波动率分析、蒙特卡洛盈亏分布、结构扫描。
- PyInstaller 打包与 GitHub Release 流水线：Windows x86_64 与 macOS arm64 免安装包。
- 修复冻结包下 numpy 2.x 的导入失败（收集 numpy / pandas 子模块与动态库）。

[2.0.0]: https://github.com/Grefer/DeltaLab/compare/v1.1.0...v2.0.0
[1.1.0]: https://github.com/Grefer/DeltaLab/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/Grefer/DeltaLab/releases/tag/v1.0.0
