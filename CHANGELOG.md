# 更新日志

本文件记录 DeltaLab 每个发布版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

预编译包在 [Releases](https://github.com/Grefer/DeltaLab/releases) 页下载。

---

## [2.0.0] - 2026-08-15

这一版把 DeltaLab 从「跑一次回测看结果」变成「把多次回测横向比出结论」。

新增 **🆚 结果对比** 和 **🎯 策略优选** 两条完整功能线，各自有独立的落盘格式、缓存层和口径规则；`gui_app.py` 拆成分层的 `deltalab_ui/` 包；测试从 23 条扩到 945 条。相对 v1.1.0 共 36 个提交、约 +39,000 行。

### 新增

#### 🆚 结果对比与回测结果池

- **保留任意次回测结果并即时对照**。回测成功后点 `＋ 保留当前结果到对比` 存一份轻量快照，勾选任意组合即可对照累计曲线与绝对指标（期末净损益、总成本、最大回撤、触发 / 成交次数、换手额）。切换勾选只读缓存，不重跑回测、不重建图表控件。
- **本页不替你下判断**，任意组合都能勾——买卖方向、不同期权、不同行情放进同一张表都不拦，页面负责**说清这次的变量差在哪一项**，并提示会误导读数的情况。
- **自动存本机、重开仍在**。开发态写 `data/backtest_pool/`，冻结包写 `~/.deltalab/pool/`（`.app` 包与安装目录可能只读）。每条一个 gzip JSON 包，最多 20 条，超出按序号淘汰最旧的。
- **包内自带重放配方**（行情切片 + 冻结的运行参数），所以「加载明细」不依赖 CSV 原文件是否还在、也不依赖 Wind 能否联网。bar 级明细另存一份可丢弃的内容寻址缓存，与策略优选下钻共用。区别是：包不可重建所以必须留档，缓存可重建所以删了无妨。
- **结果池操作**：显示 / 隐藏、重命名（改名不丢勾选与数据）、全选 / 取消全选、删除选中、全部清空、行右键菜单（显示 / 参数详情 / 加载明细 / 应用策略 / 重命名 / 删除）。删除作用于**行选择**而非勾选，避免「全选」之后一点删除清空整池。
- **参数详情**只读列出该快照的全部原始输入，取值一律回译成中文（`-1` → `买入`，`close_to_close` → `每日收盘`），合约参数按该快照自己的期权大类完整列出。
- 图上最多同时叠 8 条曲线；标签页标题带已保存条数（`🆚 结果对比 (N)`）。

#### 🎯 策略优选

- **用真实历史回答「哪种触发方式更值」**。仅接受 CSV / Wind 真实行情（选模拟来源会阻止启动），从同一分析截止日向前**严格连续**回放近周 / 近月 / 近季 / 近半年 / 近年（`5 / 20 / 61 / 122 / 243` 交易日）五档，可复选。
- **固定以每日收盘 C2C 为基准**，不可关闭。单一标的按增量金额排名——`增量收益 = Σ(候选日净 PnL − C2C 日净 PnL)`、`增量信噪比 = mean(逐日增量) / std(逐日增量)`，两者一并算出、点表头切换。
- **代理段与结算**：左侧期权期限 `T` 只定义每段最长存续期，**不扩大证据区间**。`L <= T` 时整段按剩余期限公允价值 MTM；`L > T` 时按 `T` 续接，完整段到期结算、不足 `T` 的尾段 MTM。结果区披露代理段总数、到期段数、MTM 段数与实际评分日。
- **品种入口**：`P2609.DCE` 视为单个合约只分析自身，`P.DCE` 视为品种入口，按 Wind `trade_hiscode` 逐日主力映射，只加载证据区间内真实出现过的历史合约。具体期货合约用不复权行情，股票 / ETF 用前复权。
- **候选空间**：带宽统一用日波动 σ 倍数表达（默认 `0.5, 0.75, 1, 1.5, 2`，含「加入当前回测带宽」最多 10 档）；固定时刻候选默认按交易日业务顺序排为 `23:00, 11:30, 15:00`，Wind 自动跳过该代码的休市时刻。
- **资格与降级**说清楚：候选须覆盖严格区间内全部 C2C 代理段才具备正式择优资格，历史不足时只作诊断；候选均不优于基准（含持平）时明确显示「维持每日收盘」。品种合约池跨合约金额不可相加，显式降级为逐段有界 RMS 改善、按实际评分日加权，此时增量三列显示 `—` 且表头不可点。
- **结果可保存 / 载入**（开发态 `data/history_results/`，冻结包 `~/.deltalab/results/`），并可把胜出参数一键写回左侧（只写对冲策略，不动期权 / 方向 / 行情，也不代跑）。
- 排名表首列最多勾选 8 个候选，默认勾选前三名里可比的；主图按严格区间顺序拼接全部非重叠共同代理段的日 PnL，共同交集不足 `L` 日时标注实际日数，**失败段不补零**。

#### 其他新增

- **分段 bar 级明细的内容寻址缓存**（`history_bar_cache.py`）：bar 级输出平均 971 KB，而重建它所需的输入一年 1 分钟只约 206 KB，因此包里只存输入、明细走缓存。
- **智能行情粒度**：真实行情的每日 bar 数由时间索引与交易 session 自动推导，Wind 请求粒度按策略自动选择。
- **日内缓存告警**与 Wind 取数的错误处理改进。
- `.env.example`：本地凭据模板，配套 `.gitignore` 规则防止真实凭据进版本库。

### 变更

- **`gui_app.py` 拆成 `deltalab_ui/` 包**。开发期该文件一度涨到 11k 行，现已拆为 15 个模块（约 11,000 行），`gui_app.py` 收缩到 1,691 行、保留为兼容入口 re-export 公开名字，外部与测试继续写 `gui_app.XXX` 即可。两条硬规则：依赖方向单向（`gui_app` → `deltalab_ui`，包内模块一律不 import `gui_app`）；包级不做 re-export，避免 `theme` 的 import 期副作用（选 matplotlib 后端）在意想不到的时机触发。
- **指标改名**：`增量性价比` → `增量信噪比`（原名暗示成本收益比，实际算的是逐日增量的均值 / 标准差）。
- **Wind 回测入口改为「分析截至日」驱动**，起始范围按所需评分日 + 预热 + Day 0 锚点自动向前扩展。
- **策略优选周期口径重写**：从「最近 `L` 日挑若干终点、每个终点再向前回看 `T` 日」改为「从同一截止日向前严格连续回放最近 `L` 日」，不再生成或传递 `5 / 12 / 24 / 36 / 48` 的终点预算，结果区也不再展示终点间隔与相邻窗口重叠率。
- **历史候选上限 5 → 8**，默认勾选前三名。
- **σ 来源收敛**：候选带宽的 σ 恒取左侧输入的波动率，界面不再提供来源选项——择优只比带宽倍数，全部候选共用同一个 σ 才可比（引擎仍支持 `realized`，但不从 GUI 进入）。
- **移除行情粒度的手工调粗**：调粗只会静默漏掉 bar 内触发，让结论偏乐观。
- 表单改用统一布局系统（共享列宽）；主题 / 字体 / 布局初始化抽到 `deltalab_ui/theme.py`；新增现代化复选框实现；历史按钮与标签的视觉层次调整。

### 修复

- **内容寻址缓存的键计算缺陷**：不同输入可能命中同一份缓存，会串用别的段的 bar 明细。
- **策略优选校验的 `UnboundLocalError`**，以及排序键混用不同量纲导致的排名错误。
- **品种池模式未显式降级排名口径**：跨合约金额被直接相加。
- **回放策略未按分段成功过滤**，以及预热参数未持久化。
- 状态栏打包顺序（需在 body 创建之前）导致的布局问题。
- 历史页分割线定位。
- 无显示环境下的 headless import 失败。

### 测试与 CI

- 测试从 **23 条（2 个文件）扩到 945 条（12 个文件）**——416 条纯逻辑 + 529 条 GUI。新增结果池 / 结果包 / bar 缓存的编解码与边界测试、对冲策略测试、GUI 策略状态与触发明细测试。
- CI 在 ubuntu × Python 3.10 / 3.13 上跑全量：先跑纯逻辑批（失败信息最干净），再用 `xvfb-run` 跑 GUI 批。
- `pyproject.toml` 把 pytest 收集范围限定在 `tests/`，裸跑 `pytest` 即可开箱可用。

### 升级说明

- **无需迁移**。结果池与策略优选结果包都是本版新增，v1.x 不存在这两类文件。
- 两类结果包各带 `schema_version`（结果池 `1`，优选结果包 `2`），**版本不符时拒绝载入而不是硬渲染**——字段口径改过不止一次，用新代码渲染旧包会静默给出错误结论。被跳过的条目会在空状态区列出原因。
- 三个运行时目录（`data/cache/`、`data/backtest_pool/`、`data/history_results/`）已写进 `.gitignore`：结果池包内含 `csv_path` 全路径与 `wind_code`，绝不能进版本库。
- 缓存能重建，删了无妨；结果包不可重建（Wind 区间随分析截至日移动，同样参数换个月跑出来不是同一段数据），必须留档。
- CI 产出的发布包不带 WindPy（GitHub runners 上没有 Wind 终端）。要让发布包支持 Wind，需在装有 Wind 终端的机器上本地打包，见[使用文档 §1.3](docs/GUI_USAGE.md#13-可选依赖)。

---

## [1.1.0] - 2026-05-22

### 新增

- **雪球期权**（`Option_SNB`）与 `trade_calendar` 交易日模块。
- 累计期权新增 `Opt_Decumulator`（普通累计）与 `Opt_Decumulator_Fix_E`（固赔到期结算累计）两种定价方法。
- PyInstaller 打包支持 WindPy：新增 runtime hook 确保冻结包里 DLL 能正确加载，并给出详细的错误提示。

### 修复

- Windows 打包版因 `charmap` 编码崩溃：spec 中强制 stdout 使用 UTF-8。
- 强制 JavaScript action 使用 Node 24，消除 Node 20 弃用警告。

---

## [1.0.0] - 2026-04-21

首个公开发布。

- 基于 Python / tkinter 的期权 Delta 动态对冲回测 GUI。
- 5 大类期权（香草 / 累计 / 亚式 / 气囊 / 雪球）、3 种数据源（蒙特卡洛 / CSV / Wind API）。
- 6 宫格对冲图表、波动率分析、蒙特卡洛盈亏分布、结构扫描。
- PyInstaller 打包与 GitHub Release 流水线：Windows x86_64 与 macOS arm64 免安装包。
- 修复冻结包下 numpy 2.x 的导入失败（收集 numpy / pandas 子模块与动态库）。

[2.0.0]: https://github.com/Grefer/DeltaLab/compare/v1.1.0...v2.0.0
[1.1.0]: https://github.com/Grefer/DeltaLab/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/Grefer/DeltaLab/releases/tag/v1.0.0
