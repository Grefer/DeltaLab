# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Local secrets (keep the placeholder template tracked)
.env
.env.*
!.env.example

# Jupyter Notebook
.ipynb_checkpoints/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Archive (old duplicated files)
archive/

# Data cache (runtime generated)
data/cache/

# 策略优选结果包（用户产出，不是缓存：Wind 区间随分析截至日移动，
# 同样参数换个月跑出来不是同一段数据，删了无法重建）
data/history_results/
*.parquet

# Test cache
.pytest_cache/

# Marketing / promo assets (not part of the product)
tools/make_xhs_cards.py
assets/xhs_dark/
assets/xhs_light/

# AI assistants
.gemini/
.claude/
CLAUDE.md
