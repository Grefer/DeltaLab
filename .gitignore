# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Local secrets (keep the placeholder template tracked)
.env
.env.*
!.env.example

# Jupyter Notebook
.ipynb_checkpoints/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Archive (old duplicated files)
archive/

# Data cache (runtime generated)
data/cache/
data/logs/

# 策略优选结果包（用户产出，不是缓存：Wind 区间随分析截至日移动，
# 同样参数换个月跑出来不是同一段数据，删了无法重建）
data/history_results/

# 回测结果池（自动保存，包内含 CSV 全路径与标的代码，绝不能进版本库）
data/backtest_pool/
*.parquet

# Test cache
.pytest_cache/

# Marketing / promo assets (not part of the product)
tools/make_xhs_cards.py
tools/make_xhs_cards_v2.py
assets/xhs_dark/
assets/xhs_light/
assets/xhs_v2_dark/
assets/xhs_v2_light/

# AI assistants
.gemini/
.claude/
CLAUDE.md
